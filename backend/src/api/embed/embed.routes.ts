// GET / embed / blogs / { blogSlug } ? part = snippet, statistics
// GET / embed / posts / { postSlug } ? part = snippet, contentDetails
// GET / embed / search ? part = snippet & blogSlug={ slug }& q={ query }
